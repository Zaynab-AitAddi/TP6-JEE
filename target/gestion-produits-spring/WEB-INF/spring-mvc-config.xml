<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--
    ============================================================
    CONFIGURATION SPRING MVC
    ============================================================
    Ce fichier remplace tout le web.xml MVC2 qui listait chaque servlet.
    Spring scanne automatiquement les packages et découvre :
      @Controller  → les contrôleurs
      @Service     → les services métier
      @Repository  → les DAO
    ============================================================
    -->

    <!-- 1. Découverte automatique des composants Spring -->
    <context:component-scan base-package="dao, metier, util, web" />

    <!-- 2. Active les annotations @RequestMapping, @GetMapping, etc. -->
    <mvc:annotation-driven />

    <!-- 3. Servir les ressources statiques (CSS, JS, images)
            sans passer par le DispatcherServlet -->
    <mvc:resources mapping="/css/**"    location="/css/" />
    <mvc:resources mapping="/js/**"     location="/js/" />
    <mvc:resources mapping="/images/**" location="/images/" />

    <!-- 4. ViewResolver : traduit un nom de vue ("dashboard")
            en chemin JSP ("/WEB-INF/views/dashboard.jsp") -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

</beans>
